#include <stdio.h>

int main ()
{
    //sizeoff(type)

    int dim_int;
    dim_int = sizeof(int);

    float test=0.4;

    printf("il valore in termini di byte di int e': %d\n",&dim_int,test);

    return 0;
                                                                                                                                                                                                                                                                                                      
}